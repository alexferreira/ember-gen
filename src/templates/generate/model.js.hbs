var {{objectName}} = DS.Model.extend(Ember.Validations.Mixin, { {{#each fields}}
  {{name}}: DS.attr('{{type}}'){{comma}}{{/each}}{{#if fields}},{{/if}}
  validations: {
  
  }
});

module.exports = {{objectName}};

