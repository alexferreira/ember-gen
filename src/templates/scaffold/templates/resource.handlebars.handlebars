<div class="row-fluid">
  <div class="span3">
    <div class="well sidebar-nav">
      <ul class="nav nav-list">
        <li class="nav-header">Sidebar</li>
        <li><a href="#">Link</a></li>
        <li><a href="#">Link</a></li>
        <li class="nav-header">Sidebar</li>
        <li><a href="#">Link</a></li>
        <li><a href="#">Link</a></li>
      </ul>
    </div>
  </div>
  <div class="span9">
    <div class="row-fluid">
      <h2>{{title}}</h2>
      {{#each fields}}
      <p>
        <b>{{title}}:</b> \{{{{name}}}}}
      </p>
      {{/each}}

      \{{#linkTo "{{resourcesRoute}}" class="btn"}}←All {{resources}}\{{/linkTo}}
      \{{#linkTo "{{editRoute}}" model class="btn btn-primary"}}Edit\{{/linkTo}}
      <a href="#" \{{action "destroy"}} class="btn btn-danger">Destroy</a>
    </div>
  </div>
</div>
