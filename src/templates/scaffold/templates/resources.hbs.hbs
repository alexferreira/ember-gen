<div class="row-fluid">
  <div class="span3">
    <div class="well sidebar-nav">
      <ul class="nav nav-list">
        <li class="nav-header">Sidebar</li>
        <li>\{{#linkTo "{{newPath}}"}}New {{modelTitle}}\{{/linkTo}}</li>
        <li><a href="#">Link</a></li>
        <li class="nav-header">Sidebar</li>
        <li><a href="#">Link</a></li>
        <li><a href="#">Link</a></li>
      </ul>
    </div>
  </div>
  <div class="span9">
    <div class="row-fluid">
      <h2>{{title}}</h2>
      <table class="table table-striped table-bordered table-hover">
        <thead>{{#each fields}}
          <th>{{name}}</th>{{/each}}
        </thead>
        <tbody>
          \{{#each {{resource}} in controller}}
          <tr>{{#each fields}}
            <td>\{{{{../resource}}.{{name}}}}}</td>{{/each}}
            <td>\{{#linkTo "{{resource}}" {{resource}}}}}view\{{/linkTo}}</td>
          </tr>
          \{{/each}}
        </tbody>
      </table>
    </div>
  </div>
</div>
